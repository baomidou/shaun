dependencies {
    api "${lib.'pac4j-http'}"
    api "${lib.'pac4j-jwt'}"
    implementation "${lib.'pac4j-oauth'}"
    implementation "${lib.'pac4j-cas'}"
    implementation "${lib.'spring-web'}"
    implementation "${lib.'spring-context'}"
    implementation "${lib.'servlet-api'}"
}