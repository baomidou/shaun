shaun:
  login-path: /login
  matcher-names: xssprotection
  security:
    logout-path: /logout
    exclude-path:
      path:
        - /favicon.ico
    extractor:
      location: cookie

logging:
  level:
    com.baomidou.shaun: debug